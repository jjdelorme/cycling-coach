# v1.6.3 Analysis & Workout Robustness

## Overview
Address user feedback regarding 0 TSS workouts and UX improvements on the Seasonal Macro-Plan visualization.

## Discovered Root Causes (0 TSS / Missing Targets)
1. **Pydantic Model Stripping**: `PlannedWorkout` schema was missing `planned_tss`, causing FastAPI to strip it from JSON responses even when present in DB.
2. **Generator Strictness**: `_build_zwo_xml` skipped unknown step types (e.g., "Recovery", "Tempo"). If an AI agent used these names, the resulting XML was empty.
3. **Parser Gaps**: `_parse_zwo_steps` only looked for `IntervalsT`, skipping `Intervals` (legacy), `FreeRide`, and `Ramp`.

## Completed Tasks

### 1. Workout TSS & Target Robustness (Backend)
- [x] **Schema Fix**: Added `planned_tss` to `PlannedWorkout` in `server/models/schemas.py`.
- [x] **Robust Generator**: Updated `server/services/workout_generator.py` to map common aliases (Recovery, Rest, Tempo, etc.) to standard tags.
- [x] **Robust Parser**: Updated `server/routers/planning.py` to handle `Intervals`, `FreeRide`, and `Ramp` tags.
- [x] **Standardized Defaults**: Updated `calculate_planned_tss` to match generator defaults (0.65 for SteadyState, etc.).
- [x] **Unit Tests**: Added tests in `tests/unit/test_workout_generator.py` for calculation edge cases.

### 2. Seasonal Macro-Plan Visualization (Frontend)
- [x] **Current Week Styling**: In-progress week now uses a light green dotted line and a distinct point style (`rectRot`).
- [x] **Today Line**: Added a "TODAY" label to the vertical indicator line.
- [x] **Stats Context**: Added total week counts (e.g., "2 / 14") and a subtitle explaining the calculation range.

### 3. Sync Protection (Backend)
- [x] Added `INTERVALS_ICU_DISABLED` to `server/config.py` (reads from `INTERVALS_ICU_DISABLE` env var).
- [x] Updated `server/services/intervals_icu.py` to block all "push" operations (workout upload, event delete, FTP/Weight update) when disabled.
- [x] Ensured ride downloads still work when disabled by keeping `is_configured` independent of the disable flag.
- [x] Added unit tests in `tests/unit/test_intervals_icu_disable.py`.

## Verification
- [x] DB check confirmed TSS exists but was missing in API responses.
- [x] Schema fix verified to expose `planned_tss`.
- [x] Parser/Generator aliases verified to prevent empty XMLs.
- [x] Visual verification of Analysis page improvements.
- [x] Unit tests for Sync Protection passing.
