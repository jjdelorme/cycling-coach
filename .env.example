# PostgreSQL connection string
# Local dev (via podman-compose):
DATABASE_URL=postgresql://postgres:dev@localhost:5432/coach

# intervals.icu integration (optional)
INTERVALS_ICU_API_KEY=
INTERVALS_ICU_ATHLETE_ID=

# AI model (optional, defaults to gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Withings integration
WITHINGS_CLIENT_ID=
WITHINGS_CLIENT_SECRET=
WITHINGS_REDIRECT_URI=http://localhost:8000/api/withings/callback
